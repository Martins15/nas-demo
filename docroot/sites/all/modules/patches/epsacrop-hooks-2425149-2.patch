diff --git a/epsacrop.api.php b/epsacrop.api.php
new file mode 100644
index 0000000..3128f32
--- /dev/null
+++ b/epsacrop.api.php
@@ -0,0 +1,50 @@
+<?php
+/**
+ * @file
+ * Hooks provided by the epsacrop.
+ */
+
+/**
+ * @addtogroup hooks
+ * @{
+ */
+
+/**
+ * This hook is invoked from epsacrop_crop_image() before the image will be updated.
+ *
+ * @param object $image
+ *   Image object.
+ *
+ * @param array $settings
+ *   Image settings.
+ *
+ * @param array $coords
+ *   Image coords.
+ *
+ * @param string $style_name
+ *   Styles name.
+ */
+function hook_epsacrop_crop_image_presave($image, $settings, $coords, $style_name) {
+}
+
+/**
+ * This hook is invoked from epsacrop_crop_image() when the image was updated.
+ *
+ * @param object $image
+ *   Image object.
+ *
+ * @param array $settings
+ *   Image settings.
+ *
+ * @param array $coords
+ *   Image coords.
+ *
+ * @param string $style_name
+ *   Styles name.
+ */
+function hook_epsacrop_crop_image_postsave($image, $settings, $coords, $style_name) {
+}
+
+/**
+ * @} End of "addtogroup hooks".
+ */
diff --git a/epsacrop.module b/epsacrop.module
index eb9084b..72e6707 100644
--- a/epsacrop.module
+++ b/epsacrop.module
@@ -335,13 +335,24 @@ function epsacrop_crop_image(stdClass $image, $settings) {
     if (!empty($coords) && is_array($coords)) {
       $style_name = _epsacrop_get_style_name_from_url();
       if (!empty($style_name)) {
+
+        // Trigger presave hook.
+        module_invoke_all('epsacrop_crop_image_presave', $image, $settings, $coords, $style_name);
+
         $style = _epsacrop_load_style($style_name);
         $effect = _epsacrop_get_effect($style);
         $preset = 'epsacrop-' . $effect['ieid'] . '-' . $effect['isid'];
         $coord = $coords[$fid][$preset];
         if (!empty($coord)) {
           if (image_crop($image, $coord['x'], $coord['y'], $coord['w'], $coord['h'])) {
-            return image_resize($image, $settings['width'], $settings['height']);
+            $proceed = image_resize($image, $settings['width'], $settings['height']);
+
+            // Trigger postsave hook.
+            if ($proceed) {
+              module_invoke_all('epsacrop_crop_image_postsave', $image, $settings, $coords, $style_name);
+            }
+
+            return $proceed;
           }
         }
       }
